{% extends 'base.html.twig' %}

{% block title %}ProductCat{% endblock %}

{% block content %}
    <h1>ProductCat</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ product_cat.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ product_cat.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_product_cat_index') }}">back to list</a>

    <a href="{{ path('app_product_cat_edit', {'id': product_cat.id}) }}">edit</a>

    {{ include('product_cat/_delete_form.html.twig') }}
{% endblock %}
